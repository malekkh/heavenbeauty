name: Deploy to Vercel

# PRs get an isolated Preview deploy; pushes to main go to Production.
on:
  push:
    branches: [main]
  pull_request:

# Project identifiers (from `.vercel/project.json` after `vercel link`).
# The Vercel CLI reads the VERCEL_ORG_ID / VERCEL_PROJECT_ID env vars; we source
# them from the repo secrets you created (no underscores).
env:
  VERCEL_ORG_ID: ${{ secrets.VERCELORGID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCELPROJECTID }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Quality gate — fail the deploy if lint/types/build break.
      - run: pnpm lint
      - run: pnpm typecheck

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      # ---- Preview (pull requests) ----
      - name: Deploy Preview
        if: github.event_name == 'pull_request'
        run: |
          vercel pull --yes --environment=preview --token=${{ secrets.VERCELTOKEN }}
          vercel build --token=${{ secrets.VERCELTOKEN }}
          url=$(vercel deploy --prebuilt --token=${{ secrets.VERCELTOKEN }})
          echo "Preview: $url"
          echo "PREVIEW_URL=$url" >> "$GITHUB_ENV"

      # ---- Production (push to main) ----
      - name: Deploy Production
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          vercel pull --yes --environment=production --token=${{ secrets.VERCELTOKEN }}
          vercel build --prod --token=${{ secrets.VERCELTOKEN }}
          vercel deploy --prebuilt --prod --token=${{ secrets.VERCELTOKEN }}
